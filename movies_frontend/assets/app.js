(function(){
  // Global app JS for all generated screens
  // This can host event delegation or utilities shared by screens.
  window.FigmaScreenRuntime = window.FigmaScreenRuntime || {
    init: function(){ /* no-op for now */ }
  };
  document.addEventListener('DOMContentLoaded', function(){
    window.FigmaScreenRuntime.init();
  });
})();
